/**
 * EVOLVE ATLAS — Update Anagrafica (public export)
 * -----------------------------------------------
 * Esporta la mutation “update” in stile PATCH (delta + unset).
 */
export * from "./update";
